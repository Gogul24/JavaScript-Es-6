const name1 = "Imarticus";
console.log(name1);
const name1 = "learning";//shows error 
console.log(name1);
//we can use 'let' to mutate the variable but we cant use const to mutate variable

//we can use 'var' to redeclare and reassign the value

/* let name ;
var name; - variable declaration
const name ="aiegf"; - varibale declaration and initialization 